<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- YOUR ORIGINAL SPLASH THEMES (KEEP THEM) -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>

    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>

    <!-- RAZORPAY MAGIC THEME (ADDED BELOW) -->
    <!-- THIS FIXES: Manifest merger failed + White screen + -4 FPS -->
    <style name="CheckoutTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">#512DA8</item>          <!-- Purple header -->
        <item name="colorPrimaryDark">#3700B3</item>     <!-- Dark purple status bar -->
        <item name="colorAccent">#FF4081</item>          <!-- Pink buttons -->
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:textColorPrimary">#000000</item>
        <item name="android:textColorSecondary">#757575</item>
        <item name="android:windowAnimationStyle">@null</item>
    </style>

</resources>